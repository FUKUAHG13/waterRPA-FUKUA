# 断点与逐步调试

## Status

Completed

## Comments

- 调试会话使用条件变量协调 Worker 与 UI，停止、继续和单步均不轮询 Qt 控件。
- 暂停/恢复写入有界运行轨迹；变量值仅用于暂停界面，不进入诊断报告。
- 单独执行选中步骤会强制清除断点，避免无人继续时挂起。

## Acceptance

- 步骤设置可保存断点，主列表有明确“断”标记，F8 可快速切换。
- 断点发生在步骤执行前；停止可立即解除等待。
- 支持暂停下一步、继续到下一断点和单步越过。
- 暂停时间不计入全局运行时限，运行轨迹记录暂停/恢复但不记录步骤参数。
